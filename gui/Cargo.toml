[package]
name = "osedax-gui"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "osedax-gui"
path = "src/main.rs"

[dependencies]
osedax-core = { path = "../core" }
# egui/eframe added when GUI work begins (deferred)
