fn main() {
    // GUI is deferred (egui/eframe). This stub keeps the crate in the
    // workspace so the architecture accommodates it from the start.
    eprintln!("osedax-gui is not implemented yet");
}
